modules = ["nodejs-20", "web"]
run = "npm run dev"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "cd api && pip install fastapi uvicorn && uvicorn main:app --host 0.0.0.0 --port 8000"]
build = ["sh", "-c", "npm run build"]

[[ports]]
localPort = 8080
externalPort = 80
